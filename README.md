# Ecommerce App Test

Full-stack ecommerce application generated by the Claude Ecommerce Spec-to-Code Generator.

## Stack

- **Backend**: NestJS (TypeScript) — REST API with JWT auth, RBAC
- **Frontend**: Next.js 14 (App Router) — Tailwind CSS, shadcn/ui
- **Database**: PostgreSQL with TypeORM migrations
- **Cache**: Redis
- **Infrastructure**: Docker Compose (5 services)

## Quick Start

```bash
cd infra
cp .env.example .env
docker compose up -d
```

API: http://localhost:3000/api/v1
Swagger: http://localhost:3000/api/docs
Frontend: http://localhost

## Generated Features

- Product Catalog (products, categories, variants, inventory)
- Shopping Cart (persistent, stock-aware)
- Order Management (state machine: pending → confirmed → shipped → delivered)
- Customer Management (registration, JWT auth, addresses)

## Test Results

- E2E Tests: 37/37 PASS (Playwright)
- Code Review: APPROVE_WITH_MINOR_CHANGES (0 CRITICAL, 0 HIGH)
